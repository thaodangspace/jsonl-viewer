# Allowed root folders for filesystem access (comma-separated)
# The filesystem API will only serve files under these directories
ALLOWED_ROOT_FOLDERS=/Users/dt/code

# LM Studio local LLM (for translation feature)
# LM Studio must be running with a model loaded
LMSTUDIO_URL=http://localhost:1234
LMSTUDIO_MODEL=gemma-4-26b-a4b-it-optiq
