<script>
  // Runtime icon registry for dynamic icon selection (e.g. file-type mappings).
  // Only icons actually used dynamically are imported here; static icons use
  // direct ~icons/lucide/... imports in their respective components.
</script>
